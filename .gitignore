# macOS AppleDouble / resource fork metadata
._*
.claude
CLAUDE.md

# Obsidian knowledge base (personal notes, not for repo)
knowledge/

# Node.js dependencies
node_modules/

# EMQX runtime credentials (never commit)
infrastructure/emqx/credentials.env
