# Database (PostgreSQL). Same as Java service.
# Optional: use same DB as Spring service to reuse data.
DATABASE_URL="postgresql://access_control_service:access_control_service@localhost:5432/access_control_service?schema=public"

# Server port (default 8085)
PORT=8085
# or
# ACCESS_CONTROL_SERVICE_PORT=8085
