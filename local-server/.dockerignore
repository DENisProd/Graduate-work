# Rust build artifacts (each service uses its own context, but belt-and-suspenders)
**/target

# JS deps and previous build output
local-frontend/node_modules
local-frontend/dist

# Not needed for caddy build context
local-backend
modbus-mqtt-bridge
mosquitto
zigbee2mqtt
.git
